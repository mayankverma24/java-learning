package sortingSearching;

public class QuickSort {/*
						 * public static void main(String[] args) { int[] arr = { 64, 34, 25, 12, 22,
						 * 11, 90 }; int n = arr.length;
						 * 
						 * }
						 * 
						 * low --> Starting index, high --> Ending index public int quickSort1(arr[],
						 * low, high) { if (low < high) { pi is partitioning index, arr[pi] is now at
						 * right place pi = partition(arr, low, high);
						 * 
						 * quickSort(arr, low, pi - 1); // Before pi quickSort(arr, pi + 1, high); //
						 * After pi } }
						 */
}
